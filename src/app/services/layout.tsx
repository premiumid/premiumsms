import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Services — PremiumID',
  description: 'Browse 2,500+ services for real SIM SMS verification. Live prices and stock for WhatsApp, Telegram, Google, and more.',
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
